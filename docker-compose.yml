version: '3.8'

services:
  pixel-gemini-bot:
    build: .
    container_name: pixel-gemini-bot
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
